# AI Usage and Verification Log

The assignment requires transparent documentation of AI assistance. Record actual usage only. Do not claim a test, review, or manual change that did not occur.

## Logging format

| Date | Task | Tool | Prompt summary or reference | AI contribution | Candidate decision or modification | Verification | Result |
|---|---|---|---|---|---|---|---|
| 2026-08-18 | Requirement and validator analysis | ChatGPT | Compared the assignment PDF, sample data, sample trace, and `validate_run.py` | Identified the concurrency and finish-time conflict between the PDF and validator | Chose a validator-compatible default mode with no per-server overlap, pending clarification | Inspected validator source and ran the supplied sample trace through it | Sample validated successfully; conflict recorded in specification |
| 2026-08-19 | AI-controlled delivery planning | ChatGPT | Designed a three-day milestone plan and a ChatGPT review plus Claude Code implementation workflow | Proposed source-of-truth files, quality gates, review checkpoints, and AI logging structure | Candidate selected Claude Code as implementation tool with ChatGPT for prompts and review | Candidate review pending; no application code generated in this step | Control pack created |
| 2026-08-19 | Day 1: simulation engine, adapters, repository, tests, correction pass | ChatGPT + Claude Code | `prompts/02_simulation_engine.md` | ChatGPT: independent assignment/validator analysis, architecture critique of the initial Day 1 implementation (ran 38 unit/integration tests, fed the sample and 200 randomized simulations through the real `validate_run.py`, reviewed the diff), and designed the correction-pass prompt (packaging fix, empty-`requests.csv` rejection boundary, `tick_seconds` validation, required-column validation, explicit sort, strengthened terminal-state test, atomic-publish-failure test, trailing newlines). Claude Code: implemented `backend/app/{domain,adapters,repository,services,api}`, authored and ran all tests, ran the real validator and determinism/package-install checks, wrote the completion reports. | Candidate approved both the initial Day 1 scope and the correction pass; set every hard constraint (integer `ceil_div`, explicit `SimulationDeadlockError` over `assert`, empty-input contract, genuine equal-runtime tie-break test, `provided/` copy-only with originals untouched, no git operations delegated); reviewed Claude Code's diffs and reports before approving each step; did not author any of the generated code by hand. | `pytest` 43/43, real `validate_run.py` exit 0 on the generated sample ("RESULT: VALID"), SHA-256 repeat-run determinism (`225b3f69a060d1821c7756e40830a9274f595b516eeb74e3ff0bf0ca75201845`), clean `pip install -e backend`, `git diff --check` clean — all executed and reported by Claude Code, reviewed by the candidate. | Day 1 mandatory backend foundation complete, frozen at Git tag `day1-complete` (commit `ce03b29`). No commit or tag created by Claude Code itself — Git remained under candidate control throughout. |
| 2026-08-19 | Day 2A: server CRUD + simulation API, locking, seeding, malformed-trace hardening | ChatGPT + Claude Code | `prompts/03_backend_api.md` | ChatGPT: adversarial review of the Day 2A plan and each successive implementation pass — flagged the empty-server-config vs. empty-request-input asymmetry, the CRUD read-modify-write lost-update window, the `time.sleep`-based concurrency test's weakness, the missing schema/lifecycle validation on reconstructed traces (including the ARRIVED→DROPPED→STARTED and ARRIVED→STARTED→DROPPED contradictions that a first hardening pass missed), the non-UTF-8-trace gap, and — via its independent diff review — identified a stale `docs/DAY2_PLAN.md` file that Claude Code's own file-list summary had omitted; designed all three correction-pass prompts (mandatory amendments, first hardening pass, this final pass), including the parameterized malformed-trace test list and the deterministic lock-instrumentation requirement. Claude Code: implemented the `create_app`/`lifespan` factory, RLock-protected repository CRUD, non-blocking run lock, pure `SimulationSummary`, strict two-file seeding, `JsonlTraceWriter` schema+lifecycle validation (including the STARTED/DROPPED-ordering fixes and UTF-8 decode translation), error-to-status mapping, all associated tests (112 total after this final pass, 37 of them in the dedicated malformed/corrupted-trace file), and ran every verification command. | Candidate approved Day 2A scope and all three correction passes; mandated isolated-`Settings`-only testing (never touching real `backend/data`), corrected the `docs/DECISIONS.md` numbering, required non-sleep-based deterministic concurrency proof, required timeout-guarded blocking tests, approved PUT-full-replace/500-on-corrupt-trace/seeding-scope policy choices; reviewed ChatGPT's diff-review finding on the stale `docs/DAY2_PLAN.md` file and approved its removal; reviewed Claude Code's diffs and reports at each step; retained Git and command-execution control throughout — Claude Code never committed or tagged anything. | `pytest` 112/112 (including 37 malformed/corrupted-trace and positive-lifecycle cases, plus a deterministic lock-instrumentation test), real `validate_run.py` exit 0 on an API-triggered trace, SHA-256 determinism unchanged (`225b3f69a060d1821c7756e40830a9274f595b516eeb74e3ff0bf0ca75201845`) even through the full HTTP/lock/summary pipeline across all three passes, clean `pip install -e backend[dev]` (incl. `httpx`), `git diff --check` clean, real `backend/data/run.jsonl` hash unchanged before/after every pass, manual 7-endpoint + `/health` walk. | Day 2A backend complete, including two corrective hardening passes. No Day 2B (frontend/Docker). Claude Code did not commit or tag; Git actions remained under candidate control. |
| 2026-08-20 | Day 2B-1 + Day 2B-2: React/TS/Vite dashboard, race-condition hardening, Docker images, Compose, nginx, dependency bounds, README/ARCHITECTURE documentation, full E2E verification | ChatGPT + Claude Code | `prompts/04_frontend_docker.md` (Day 2B-2 instruction, verbatim; the Day 2B-1 frontend-scaffolding instruction and its one correction-pass instruction predate this file and were not separately archived) | Claude Code: implemented all 16 frontend source/test files in its initial Day 2B-1 pass, and during its own initial verification (running the test suite and production build itself, before any external review) independently discovered and fixed two real bugs — a vitest/vite version mismatch causing a `tsc` type-conflict build failure, and a native HTML constraint-validation issue where `min`/`step` on the number inputs silently blocked form submission before the app's own JS validation ran (fixed with `noValidate`). ChatGPT: reviewed the resulting initial Day 2B-1 diff and identified three further issues — stale-async-response races in both `ServerList` and `RunPanel` (no protection against an older `GET` resolving after a newer one), a `ServerForm` test that only exercised a pre-normalized error prop rather than genuinely exercising `client.ts`'s 422-array normalization, and a few files missing a final newline — then designed one focused Day 2B-1 correction-pass prompt covering exactly those three findings, and separately designed the Day 2B-2 Docker/E2E prompt including its exact verification matrix. Claude Code then implemented the generation-token race-condition fix (with deterministic StrictMode+deferred-promise tests, no timers) for that one correction pass, plus `backend/Dockerfile`, `frontend/Dockerfile`, `frontend/nginx.conf`, `docker-compose.yml`, root `.dockerignore`, `backend/pyproject.toml` version ceilings, `backend/.gitignore` runtime-seed entries, `README.md`, `docs/ARCHITECTURE.md`, and ran the entire Day 2B-2 verification matrix below — including diagnosing and fixing a further real bug found during that run (BusyBox `wget` resolving `localhost` to `::1` first and getting refused, since nginx only listens on IPv4 — fixed by targeting `127.0.0.1` explicitly). | Candidate approved the initial Day 2B-1 scope, approved the one Day 2B-1 correction pass ChatGPT's review produced (deterministic race tests, no `time.sleep`, a real 422-normalization test through `client.ts` rather than a pre-normalized prop, final newlines), and approved the Day 2B-2 scope (exact allowed file list, editable-install requirement, `--workers 1` hardcoded, seeding-ignore rules, dependency-ceiling policy, data-safety rules for E2E); reviewed diffs and every completion report; retained Git control throughout — Claude Code never staged, committed, or tagged anything. | Frontend: `npm run test` 38/38 passed, `npm run build` (tsc clean + Vite production build) succeeded, `npm audit` 0 vulnerabilities, single deduplicated `vite@6.4.3` confirmed via `npm ls --all`. Backend: `pytest` 112/112 passed in a clean temporary venv, `pip check` clean. Docker: `docker compose config` valid, `docker compose build --no-cache` succeeded for both images, both containers reached `healthy`. Proxy E2E through port 3000: server list/create/update/delete/restore all returned expected status codes (200/201/200/204/200); triggered run returned 200 with the known 4-request summary; downloaded `run.jsonl` byte-identical to the published file. Official validator run via `docker compose exec -T backend python /app/provided/validate_run.py ...`: exit 0, `RESULT: VALID`, summary matching the sample. SHA-256 of `backend/data/run.jsonl` before Docker, after the API-triggered container run, and after `docker compose restart backend`: `225b3f69a060d1821c7756e40830a9274f595b516eeb74e3ff0bf0ca75201845` throughout, unchanged. Restart persistence: `GET /api/servers` and `GET /api/simulations/latest` identical before and after `docker compose restart backend`. Runtime image inspection: no `backend/tests`, no `pytest`/`httpx` importable in the backend image; no `node` binary, no frontend source in the final frontend image; exactly one uvicorn process confirmed via `/proc`. **Browser verification: not performed — all verification above was automated (curl/proxy/container-exec), not a real browser.** A manual checklist was provided to the candidate instead. **Clean-clone verification: not performed and explicitly deferred — these changes are uncommitted; a true clean-clone test cannot honestly include uncommitted work, and no commit was made solely to enable one.** | Day 2B-1 and Day 2B-2 complete pending candidate review. No bonus features started. No commit, tag, or push made by Claude Code — all Git actions remain the candidate's to perform. |
| 2026-08-20 | Day 3A bonus metrics and interchangeable scheduling strategies, including hardening and final consistency corrections | ChatGPT + Claude Code | `prompts/05_bonus_metrics_strategies.md` | Claude Code implemented the strategy registry, validator-compatible `lowest_id` strategy, strategy API, strategy-aware simulation route, pure metrics computation, verified `run_context.json` lifecycle, exact persisted-byte hashing, `MetricsPanel`, `StrategySelector`, cross-panel refresh behavior, tests, documentation, and Docker verification. ChatGPT designed and reviewed the Day 3A scope and performed adversarial diff review; its correction prompts covered strict run-context validation, CRLF exact-byte hashing, negative queue-depth rejection, trace-only per-server visibility, registry duplication, validator subprocess encoding, canonical-hash regression coverage, documentation contradictions, `Literal`-based strategy duplication, and bool `schema_version` acceptance. Claude Code implemented the resulting corrections and ran the complete verification matrix. ChatGPT then diagnosed a Windows Git end-of-line portability issue from the candidate's clean-clone evidence — no repository-level EOL policy overrode `core.autocrlf` for the generated trace — and designed the minimal correction prompt; Claude Code created the root `.gitattributes` rule `backend/data/run.jsonl text eol=lf`, changing no `run.jsonl` content, provided fixture, Git configuration, application code, or test file. | Approved the bonus scope and all correction passes; required the mandatory default strategy and canonical trace to remain unchanged; kept Day 3B out of scope; reviewed reports and diffs; performed the final real-browser acceptance test personally; committed the Day 3A implementation as `52021bc` and the `.gitattributes` EOL correction as `11bc0ac`; personally ran both the post-`52021bc` and post-`11bc0ac` clean-clone verifications; retained full Git control throughout — Claude Code did not stage, commit, tag, or push. | Backend `pytest`: 179/179 passed and 179 collected without external environment variables. Frontend tests: 58/58 passed; production build succeeded. Both `fastest_finish` and `lowest_id` traces passed the real provided validator. Docker Compose build, health, proxy smoke, strategy endpoints, metrics endpoint, and unknown-strategy 422 behavior passed. Default canonical SHA-256 remained `225b3f69a060d1821c7756e40830a9274f595b516eeb74e3ff0bf0ca75201845`. Candidate browser verification passed: dashboard loaded without console errors; both strategies appeared; default and selected-strategy runs worked; summary and metrics refreshed; refresh persistence and download worked; narrow viewport remained usable. First clean-clone verification (commit `52021bc`): both Docker images built successfully, both containers became healthy, both strategies ran successfully through the nginx proxy, `GET /latest/metrics` returned `context_available: true` with the correct `strategy_used`, the real provided validator returned `RESULT: VALID`, and the final default trace matched the canonical SHA-256 above — `HEAD:backend/data/run.jsonl` and the generated working file were exactly byte-identical (764 bytes, LF-only), yet Windows Git still reported `run.jsonl` as modified because no repository-level EOL policy overrode `core.autocrlf`. Second clean-clone verification (commit `11bc0ac`, after the `.gitattributes` correction): a fresh clone started completely clean; `docker compose up --build -d --wait` succeeded; backend and frontend became healthy; a default simulation completed with the expected 4/4/4/0 summary; the real provided validator again returned `RESULT: VALID`; SHA-256 remained canonical; and `git status --short` stayed completely clean even after the generated trace was published, confirming the EOL fix. | Day 3A bonus scope is implementation-complete; browser acceptance passed; post-commit clean-clone verification passed on both `52021bc` and `11bc0ac`; Windows cross-platform trace determinism is verified. Day 3B has not started. Only the final documentation commit and the `day3a-complete` tag remain candidate-controlled. |
| 2026-08-21 to 2026-08-22 | Final pre-submission audit, Medsien clarification, and a two-pass documentation-only resolution of the PDF/validator execution-model discrepancy | Claude Fable + ChatGPT + Claude Code | Independent final-audit prompt (Claude Fable, not separately archived) followed by two documentation-and-docstring-only correction prompts (Claude Code, not separately archived per repository policy) | Claude Fable performed an independent, read-only final pre-submission audit on 2026-08-21: found no blocking implementation defects, confirmed all backend and frontend tests passed, confirmed Docker/E2E and validator verification passed, and identified the PDF-vs-validator execution-model wording as the only remaining external risk at that time. ChatGPT reviewed the audit findings and helped the candidate formulate the exact clarification question sent to Medsien Engineering. Claude Code then performed two passes recording the resolution: an initial pass updating `docs/DECISIONS.md` (D-003 resolution and resolved open questions), `docs/SPEC.md` §5, `docs/ARCHITECTURE.md` §5–6, `README.md`, the prompt archive inventory in docs/AI_USAGE.md, and two docstrings (`domain/strategies.py`, `domain/errors.py`); then a focused correction pass refining the open-questions numbering/wording in `docs/DECISIONS.md`, sharpening D-011's shared-CPU wording, replacing an overly-absolute conflict sentence in `docs/SPEC.md` with precise validator-rejection wording, and correcting this row's own dates and claims. No application behavior was changed in either pass. | The candidate sent the clarification question to Medsien Engineering and received their authoritative written answer (email dated 2026-08-21). Medsien Engineering confirmed the serial, validator-compatible, one-active-request-per-server model (`validate_run.py`) is the authoritative execution model for this case study, and that the existing implementation is exactly the intended solution. The candidate approved the scope of both documentation-only passes, will review the resulting diff before committing, and retains final acceptance and Git control throughout — nothing has been committed yet. | **Independent final audit (Claude Fable, 2026-08-21):** backend `pytest` 179/179 passed, frontend tests 58/58 passed, Docker/E2E and the real provided validator all passed. **This documentation correction pass (Claude Code, 2026-08-22):** backend `pytest` rerun, 179/179 passed; real `validate_run.py` against the committed canonical trace: `RESULT: VALID`; canonical SHA-256 unchanged: `225b3f69a060d1821c7756e40830a9274f595b516eeb74e3ff0bf0ca75201845`; `git diff --check` clean; mechanically confirmed no executable Python line changed in either touched domain file. Frontend tests were not rerun during this documentation-only pass, since no frontend file was touched. | The external execution-model risk is resolved — Medsien Engineering confirmed the current implementation is the intended, authoritative solution. No Day 3B work performed. Claude Code did not stage, commit, tag, push, or communicate with Medsien in any form, and did not receive Medsien's response directly — all Git and external-communication actions remain the candidate's. Project is ready to submit once the candidate reviews and commits this documentation pass. |
| 2026-08-23 | Day 3B-1: planning (initial + corrected), shared-infrastructure micro-step, a test-only hardening pass, and the Timeline visualization bonus feature | ChatGPT + Claude Code | `prompts/06_bonus_timeline.md` (the approved Day 3B-1 implementation instruction; the two planning-mode passes that preceded it are not separately archived, per the same policy already applied to prior correction passes) | Claude Code produced an initial Day 3B plan (Timeline + auto-scaling) from a full read of the current codebase. ChatGPT then reviewed that initial plan and identified several defects before any implementation began, including physically-impossible auto-scaling walkthrough fixtures (synthetic metrics that no real engine trace could ever produce) and the event-order/queue-depth/trace-reader design gaps; the candidate incorporated that review into a detailed correction instruction, and Claude Code produced a corrected plan addressing all of it (persisted vs. canonicalized event order, an exact sparse queue-depth contract, a typed `trace_reader` result, a revised auto-scaling availability/policy design, and — directly from ChatGPT's plan-stage review — corrected, engine-derivable manual-walkthrough fixtures — the auto-scaling half remained design-only at this point, not implemented). Per the corrected plan's own stop condition, Claude Code first implemented a shared-infrastructure micro-step (`domain/queue_depth.py`, `services/trace_reader.py`, a behavior-preserving refactor of `domain/metrics.py`/`get_latest_metrics`) and reported a diff for review; then, on request, a test-only hardening pass (a dense-reference cross-check test, a `compute_metrics`/`compute_queue_depth` equivalence test, an invalid-UTF-8 trace-reader test, strengthened pending/hash-mismatch assertions) that exposed no production defect. With both approved, Claude Code implemented Day 3B-1 Timeline: `domain/timeline.py`, `TimelineResponse`/related Pydantic schemas, `GET /api/simulations/latest/timeline`, the `TimelinePanel` frontend (server-lane Gantt, per-request waiting/running/dropped lifecycle strip, queue-depth chart, filterable event table, unconditional accessible table, fixed-viewBox SVG geometry), backend and frontend tests, and the D-021/§28–29/README documentation updates. A focused external review of that Timeline implementation (not the plan — the plan-stage fixture review above was separate and earlier) then identified further gaps — an arbitrary `random.shuffle` permutation test that could produce lifecycle-invalid orderings, and an incomplete SVG accessibility contract (no `<desc>`, no distinct arrival/start/finish markers, color-only waiting/running distinction, no queue-depth scale or semantic table) — which Claude Code corrected in a first correction pass: a hand-built lifecycle-valid `NON_CANONICAL_ORDER` fixture, `<title>`+`<desc>` on every SVG, circle/triangle/square lifecycle markers, a diagonal-stripe waiting texture, an expanded six-item legend, queue-depth `0`/peak scale labels and a semantic Tick/Depth table, a strict-read-only API test, an exact-events-vs-deserialized-trace test, pending/hash-mismatch context tests, and an invalid-UTF-8 test. A second, final micro-correction pass then fixed a real bug the external review caught in the *test-side* sparse-queue reconstruction helper (`_peak_and_avg_from_sparse_points` omitted the final interval through `end_tick`, which happened to be masked by the canonical sample's zero final depth), fixed a genuine same-tick visual-overlap defect in `RequestLifecycleChart` (arrival and start markers shared one y position and could render on top of each other whenever `wait_ticks === 0`), synced the legend's CSS so marker classes color plain HTML spans (not just SVG `fill`) and so the waiting swatch is visibly striped rather than a plain solid color, and extended the queue table's caption to state its sparse-persistence semantics explicitly (start/end ticks, "active until next point," "last point active through end_tick"). | The candidate rejected the first Day 3B plan draft and specified detailed corrections before approving the revised one; approved the shared-infrastructure micro-step only after reviewing its full diff; requested and reviewed the separate test-only hardening pass; then approved Day 3B-1 implementation under explicit binding constraints (no auto-scaling yet, no frozen-file changes, preserve the existing Metrics API contract, create this prompt archive file, run the full verification gate, no Git operations); reviewed reports at each step; specified both subsequent correction passes in detail (the accessibility/persisted-order/fixture corrections, then this final micro-correction pass fixing the test-helper bug, the marker-overlap defect, the legend CSS, and the queue-table caption) after reviewing the implementation and, in the second case, an external diff review; scoped every pass to an explicit allowed-file list; retains Git control throughout — nothing has been staged, committed, tagged, or pushed by Claude Code at any point; no further prompt archive file was created for either correction pass, per repository policy for correction passes. | Final totals across all Day 3B-1 passes — backend `pytest`: **220/220** passed (179 pre-existing + 13 shared-infrastructure + 6 hardening + 16 initial Timeline + 5 first-correction-pass API-boundary additions + 1 final-pass synthetic-data regression test for the sparse-queue test helper's missing-final-interval bug = 220; `--collect-only -q` confirms 220 collected). The candidate's instruction for this row anticipated 219; the actual count is 220 because correction 1 of this final pass required a synthetic (non-canonical-trace) test proving the helper's fixed bug, which cannot be folded into the existing canonical-trace cross-check test (that trace's real final depth is 0, so it cannot exercise a missing-final-interval bug) — recorded here as the true, verified number rather than the anticipated one. Frontend: `npm run test -- --run` **77/77** passed; `npm run build` (tsc + Vite production build) succeeded. Real `validate_run.py` against the committed canonical trace: `RESULT: VALID`. Canonical SHA-256 unchanged throughout every pass: `225b3f69a060d1821c7756e40830a9274f595b516eeb74e3ff0bf0ca75201845`. `git diff --check` clean; `git diff --exit-code` on `backend/data/run.jsonl` and `provided/` empty at every checkpoint. Docker/clean-clone verification for Day 3B-1 remains deferred to the Day 3B-2 gate per the approved plan, which bundles that check with the second sub-milestone — it has not been performed for Timeline specifically. | Day 3B-1 (Timeline) is implementation-complete, externally reviewed, and corrected through two focused passes; Day 3B-2 (auto-scaling) has not been started — no `domain/autoscale.py` or `AutoScalePanel` file exists. Nothing has been staged, committed, tagged, or pushed by Claude Code at any point; the candidate owns scope approval, diff review, final acceptance, and every Git operation, and that review remains pending before any commit. |
| 2026-08-23 | Day 3B-2: the read-only Auto-scaling Recommendation bonus feature | ChatGPT + Claude Code | `prompts/07_bonus_autoscaling.md` (the approved Day 3B-2 implementation instruction, archived verbatim across the two messages it arrived in) | ChatGPT reviewed the corrected Day 3B design's auto-scaling policy and designed the exact implementation instruction: the first-match-wins precedence (insufficient-data and context-unavailable outranking drops; drops outranking queue/occupancy; queue-pressure-and-occupancy together outranking low-occupancy scale-down; a minimum-server-count floor), the stable reason-code set, the fixed limitations list, the exact API response shape, and the full domain/API/frontend test-scenario lists. ChatGPT then reviewed the complete 5,174-line cumulative `day3b-final.diff` after the initial implementation and found no defect in policy precedence, validator compatibility, deterministic trace generation, read-only API behavior, or the core Timeline calculations — but did identify nine focused issues, all corrected in this pass: unrestricted (plain-`str`) reason-code typing across the domain/schema/client layers; the observed response omitting `idle_configured_server_ids`; an internally-impossible `SCALE_DOWN_RESPONSE` frontend fixture (nonzero `peak_queue_depth`, which the policy's scale-down branch can never produce); Timeline test fixtures that were mutually inconsistent (a dropped-request fixture claiming verified context but no server lane; a "huge tick" fixture that relabeled only aggregate fields while keeping the small canonical event/interval list); a threshold comment in `autoscale.py` standing in for an actual module docstring; an unnamed outer `<section>` wrapper in `App.tsx` duplicating `TimelinePanel`'s own heading; overbroad "provably identical" wording for the queue-depth sparse/dense equivalence (true only for non-empty, lifecycle-complete input, not arbitrary event sequences); a missing continuation-marker sentence in the archived `prompts/07_bonus_autoscaling.md`; and ARCHITECTURE wording that mischaracterized shared-CPU execution as a remaining PDF bonus rather than an unimplemented, not-required optional experimental mode. Claude Code implemented `domain/autoscale.py` (`decide_scaling`, pure, no I/O), one necessary policy refinement beyond the literal instruction discovered while making the required "single-server → minimum_server_count" scenario reachable with real engine data (the minimum-server-count branch does not require a non-empty idle list the way scale-down does, since a lone server that ran every non-dropped request can never itself be idle — documented in `docs/DECISIONS.md` D-022), the `GET /api/simulations/latest/autoscaling` route (via the existing `read_current_run()` + `compute_metrics()`, never reimplemented), `AutoScaleObservedOut`/`AutoScaleResponse` schemas, `AutoScalePanel.tsx` (action badges, observed values, removal candidates, fixed limitations, no Apply button), backend and frontend tests, the corrected `test_api_timeline.py` comment (the old sparse-helper bug affected only the average, never the peak), and the D-022/§30/README/AI_USAGE documentation. Claude Code also ran the full verification gate including a Docker clean rebuild and proxy E2E pass. Claude Code then implemented all nine of ChatGPT's review corrections in one focused pass: a `ReasonCode` `Literal` alias threaded through `autoscale.py`, `schemas.py`, and `client.ts` (plus a Pydantic-validation test proving the model rejects an unknown code and accepts the full valid set); a real module docstring on `autoscale.py`; an "Idle configured servers" observed card in `AutoScalePanel.tsx` (`None`/`N/A`/comma-joined-ids, tested with scoped `<dt>/<dd>` assertions so duplicate `"s2"` text couldn't pass a test accidentally); a policy-coherent `SCALE_DOWN_RESPONSE` fixture (zero queue depth) with every AutoScale fixture now typed as `AutoScaleResponse`; corrected, internally-coherent `TimelinePanel.test.tsx` fixtures (a real idle server lane for the dropped-request fixture; a fully-consistent one-request/tick-1,000,000 fixture replacing the previous relabel-only one) typed as `TimelineResponse`; `App.tsx`'s outer Timeline wrapper changed from an unnamed `<section>` to a plain `<div>`; qualified queue-depth equivalence wording in `queue_depth.py`, `docs/ARCHITECTURE.md` §28, and `docs/DECISIONS.md` D-021 (non-empty, lifecycle-complete input only — arithmetic itself unchanged); the missing continuation-marker sentence inserted into `prompts/07_bonus_autoscaling.md`'s archived text; and corrected ARCHITECTURE wording describing shared-CPU execution as an unimplemented, not-required optional experimental mode rather than a remaining PDF bonus. Claude Code reran the complete verification gate (backend, frontend, validator, a second Docker clean rebuild and proxy E2E pass) afterward. | The candidate approved the full Day 3B-2 design and this implementation instruction (delivered across two messages after an earlier truncation, which Claude Code declined to guess at and asked to have resent rather than reconstructing the missing text); reviewed the preflight report; had ChatGPT perform the complete external review of the resulting `day3b-final.diff` and relayed its nine findings as this focused correction-pass instruction; will review this pass's own diff and documentation before accepting; retains complete Git control throughout — Claude Code has not staged, committed, tagged, or pushed anything at any point across either pass. Browser acceptance was explicitly deferred by the candidate's own instructions in both passes ("Stop after the report... Do not begin browser acceptance") and has not been performed. | **Initial implementation:** backend `pytest` 255/255 passed (220 pre-Day-3B-2 baseline + 20 domain `test_autoscale.py` + 15 API `test_api_autoscaling.py`); frontend `npm run test -- --run` 92/92 passed (77 baseline + 15 `AutoScalePanel`); both validator/hash/Docker checks passed (see below, superseded by this pass's rerun). **After this correction pass:** backend `pytest`: **257/257** passed (255 + 2 new `AutoScaleResponse` reason-code Pydantic-validation tests); `--collect-only -q` confirms 257 collected, no external environment variable required. Frontend: `npm ci` clean install, `npm run test -- --run` **95/95** passed (92 + 3 new idle-configured-servers-card tests), `npm run build` succeeded. Validator rerun against the canonical trace: `RESULT: VALID`. Canonical SHA-256 confirmed unchanged: `225b3f69a060d1821c7756e40830a9274f595b516eeb74e3ff0bf0ca75201845`; `git diff --exit-code` on `backend/data/run.jsonl` and `provided/` empty. Docker rerun (since frontend rendering and API schema typing both changed in this pass): `docker compose config` valid; `docker compose up --build -d --wait` reached healthy on both services via their existing bounded healthchecks (no fixed sleeps); through the nginx proxy on port 3000, exercised a default run, `/latest/timeline`, and `/latest/autoscaling` (canonical `no_change`/`steady_state` reconfirmed); `docker compose down`. This was a clean rebuild of the current working tree, **not** a post-commit clean-clone test, and **not** browser testing — only curl-based verification was performed, and no such stronger claim is made. `git diff --check` clean; `git diff --cached --name-only` empty throughout both passes. | Day 3B-2 (auto-scaling) is implementation-complete, externally reviewed by ChatGPT, and corrected through one focused pass — including two separate Docker/proxy E2E passes on the current working tree. All explicitly PDF-listed optional bonus features are now implemented: interchangeable scheduling strategies, performance metrics, timeline visualization, and the auto-scaling recommendation. Shared-CPU execution is not one of them — it remains an unimplemented, optional experimental mode outside the confirmed authoritative execution model, not a remaining PDF bonus, and is not required under D-003/D-011. Nothing has been staged, committed, tagged, or pushed by Claude Code at any point across either pass. Browser acceptance and any commit/tag/push remain the candidate's next steps. |

## Prompt archive

Store substantial prompts under `prompts/` with stable filenames. Reference the filename from the table instead of duplicating long prompt text here.

Files that currently exist in this repository:

```text
prompts/01_architecture_plan.md
prompts/02_simulation_engine.md
prompts/03_backend_api.md
prompts/04_frontend_docker.md
prompts/05_bonus_metrics_strategies.md
prompts/06_bonus_timeline.md
prompts/07_bonus_autoscaling.md
```

Naming is milestone-descriptive (`NN_topic.md`), illustrating the convention
rather than prescribing a fixed roster — later milestones name files for
what they actually cover, and files are never renamed solely to match an
earlier template.

## What to record

For each material AI-assisted milestone, record:

- What information the AI received
- What it was asked to produce
- Which suggestions were accepted or rejected
- What the candidate changed manually
- Tests, validator commands, diff review, or manual verification performed
- Remaining limitations

## Verification evidence

Prefer concrete evidence such as:

```text
pytest result
provided validator result
repeated-run hash comparison
frontend test or build result
docker compose build result
clean-clone startup result
manual API/dashboard scenario
git diff review
```

## Final AI-use summary template

```markdown
AI tools were used for requirement analysis, planning, bounded implementation,
test generation, and code review. The candidate retained ownership of architecture
and observable behavior. AI-generated changes were accepted only after diff review,
automated tests, validation with the supplied script, and manual explanation of the
relevant logic. The supplied validator and fixtures were not modified.
```
